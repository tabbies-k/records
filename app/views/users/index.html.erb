<p id="notice"><%= notice %></p>

<h1>ユーザ一覧</h1>

<table class="table">
  <thead>
    <tr>
      <th>ID</th>
      <th>email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.email %></td>
        <td><%= link_to '詳細', "/users/#{user.id}" %></td>
        <td><%= link_to '編集', "#" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新規作成', new_record_path %>
